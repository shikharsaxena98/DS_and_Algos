// Given an array of n elements, create a new array which is a rotation
// of given array and hamming distance between both the arrays is maximum.

//Hamming distance between two arrays or strings of equal length is the 
//number of positions at which the corresponding character(elements) are different.

#include<iostream>

int main(){

	return 0;
}
